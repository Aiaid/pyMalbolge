PROGRAM_START_TO ENTRY@MAIN
FLAG 1/2, FLAG0
FLAG 1/2, FLAG1
FLAG 1/2, FLAG10
FLAG 1/2, FLAG11
FLAG 1/2, FLAG12
FLAG 1/2, FLAG2
FLAG 1/2, FLAG3
FLAG 1/2, FLAG4
FLAG 1/2, FLAG5
FLAG 1/2, FLAG6
FLAG 1/2, FLAG7
FLAG 1/2, FLAG8
FLAG 1/2, FLAG9
FLAG 0/2, FLAG_CASE0
FLAG 0/2, FLAG_CASE1
FLAG 0/2, FLAG_CASE2
FLAG 1/2, FLAG_JMP
FLAG 0/2, FLAG_REV_OPR_ROT

UNIT{
59:DUP
DJMP:
60:MOV_D #(2)
REV_DJMP:
61:JMP
}
ROUTINE GLOBAL{
ENTRY:
DJMP_BASE:
  DJMP BASE
BASE:0
DJMP_CON0:
  DJMP CON0
CON0:0
DJMP_CON1:
  DJMP CON1
OPR_CON1:
  OPR CON1
ROT_CON1:
  NEXT FLAG_REV_OPR_ROT
  ROT
CON1:1743392200
  IF FLAG_REV_OPR_ROT
  BRANCH REV_OPR_CON1
  REV ROT
  SKIP END_REV_CON1
REV_OPR_CON1:
  REV OPR
  NEXT FLAG_REV_OPR_ROT
END_REV_CON1:
  NEXT FLAG1
  IF FLAG0
  BRANCH JMP_BACK0@MAIN
  NEXT FLAG0
  NEXT FLAG1
DJMP_CON2:
  DJMP CON2
OPR_CON2:
  OPR CON2
ROT_CON2:
  NEXT FLAG_REV_OPR_ROT
  ROT
CON2:3486784400
  IF FLAG_REV_OPR_ROT
  BRANCH REV_OPR_CON2
  REV ROT
  SKIP END_REV_CON2
REV_OPR_CON2:
  REV OPR
  NEXT FLAG_REV_OPR_ROT
END_REV_CON2:
  NEXT FLAG6
  IF FLAG5
  BRANCH JMP_BACK5@MAIN
  NEXT FLAG5
  NEXT FLAG6
  NEXT FLAG8
  IF FLAG7
  BRANCH JMP_BACK7@MAIN
  NEXT FLAG7
  NEXT FLAG8
DJMP_U_X:
  DJMP U_X
OPR_U_X:
  OPR U_X
ROT_U_X:
  NEXT FLAG_REV_OPR_ROT
  ROT
U_X:264
  IF FLAG_REV_OPR_ROT
  BRANCH REV_OPR_U_X
  REV ROT
  SKIP END_REV_U_X
REV_OPR_U_X:
  REV OPR
  NEXT FLAG_REV_OPR_ROT
END_REV_U_X:
  NEXT FLAG12
  IF FLAG11
  BRANCH JMP_BACK11@SUB
  NEXT FLAG11
  NEXT FLAG12
}
ROUTINE MAIN{
ENTRY:
  NEXT FLAG0
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH ROT_CON1@GLOBAL
JMP_BACK0:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH OPR_TEMP0
JMP_BACK1:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH OPR_TEMP0
JMP_BACK2:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH OPR_RETURN_ADDR@SUB
JMP_BACK3:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH OPR_RETURN_ADDR@SUB
JMP_BACK4:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH ROT_CON2@GLOBAL
JMP_BACK5:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH OPR_RETURN_TO_ADDR0
JMP_BACK6:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH ROT_CON2@GLOBAL
JMP_BACK7:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH OPR_RETURN_TO_ADDR0
JMP_BACK8:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH OPR_TEMP0
JMP_BACK9:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH OPR_RETURN_ADDR@SUB
JMP_BACK10:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH ENTRY@SUB
RETURN_TO0:
  REV_DJMP
  END
DJMP_RETURN_TO_ADDR0:
  DJMP RETURN_TO_ADDR0
OPR_RETURN_TO_ADDR0:
  OPR RETURN_TO_ADDR0
ROT_RETURN_TO_ADDR0:
  NEXT FLAG_REV_OPR_ROT
  ROT
RETURN_TO_ADDR0:RETURN_TO0
  IF FLAG_REV_OPR_ROT
  BRANCH REV_OPR_RETURN_TO_ADDR0
  REV ROT
  SKIP END_REV_RETURN_TO_ADDR0
REV_OPR_RETURN_TO_ADDR0:
  REV OPR
  NEXT FLAG_REV_OPR_ROT
END_REV_RETURN_TO_ADDR0:
  NEXT FLAG7
  IF FLAG6
  BRANCH JMP_BACK6
  NEXT FLAG6
  NEXT FLAG7
  NEXT FLAG9
  IF FLAG8
  BRANCH JMP_BACK8
  NEXT FLAG8
  NEXT FLAG9
DJMP_TEMP0:
  DJMP TEMP0
OPR_TEMP0:
  OPR TEMP0
ROT_TEMP0:
  NEXT FLAG_REV_OPR_ROT
  ROT
TEMP0:0
  IF FLAG_REV_OPR_ROT
  BRANCH REV_OPR_TEMP0
  REV ROT
  SKIP END_REV_TEMP0
REV_OPR_TEMP0:
  REV OPR
  NEXT FLAG_REV_OPR_ROT
END_REV_TEMP0:
  NEXT FLAG2
  IF FLAG1
  BRANCH JMP_BACK1
  NEXT FLAG1
  NEXT FLAG2
  NEXT FLAG3
  IF FLAG2
  BRANCH JMP_BACK2
  NEXT FLAG2
  NEXT FLAG3
  NEXT FLAG10
  IF FLAG9
  BRANCH JMP_BACK9
  NEXT FLAG9
  NEXT FLAG10
}
ROUTINE SUB{
ENTRY:
  NEXT FLAG11
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH ROT_U_X@GLOBAL
JMP_BACK11:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH L_OUTPUT
LABEL0:
  NEXT FLAG_JMP
  IF FLAG_JMP
  BRANCH DJMP_RETURN_ADDR
  END
DJMP_RETURN_ADDR:
  DJMP RETURN_ADDR
OPR_RETURN_ADDR:
  OPR RETURN_ADDR
ROT_RETURN_ADDR:
  NEXT FLAG_REV_OPR_ROT
  ROT
RETURN_ADDR:0
  IF FLAG_REV_OPR_ROT
  BRANCH REV_OPR_RETURN_ADDR
  REV ROT
  SKIP END_REV_RETURN_ADDR
REV_OPR_RETURN_ADDR:
  REV OPR
  NEXT FLAG_REV_OPR_ROT
END_REV_RETURN_ADDR:
  NEXT FLAG4
  IF FLAG3
  BRANCH JMP_BACK3@MAIN
  NEXT FLAG3
  NEXT FLAG4
  NEXT FLAG5
  IF FLAG4
  BRANCH JMP_BACK4@MAIN
  NEXT FLAG4
  NEXT FLAG5
  IF FLAG10
  BRANCH JMP_BACK10@MAIN
  NEXT FLAG10
L_OUTPUT:
  OUTPUT
  DUP
  REV OUTPUT
  IF FLAG12
  BRANCH LABEL0
  NEXT FLAG12
}

